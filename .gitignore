# ── 비밀값 ───────────────────────────────────────
# LLM API 키 · DB 접속 정보. .env는 커밋하지 않고 노션으로 별도 공유한다.
.env
.env.*
*.pem
*.key

# ── Python ──────────────────────────────────────
__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/

# ── uv / 가상환경 ────────────────────────────────
# uv.lock과 .python-version은 커밋 대상이므로 제외하지 않는다.
.venv/
venv/

# ── 테스트 · 린트 캐시 ────────────────────────────
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

# ── 런타임 산출물 ────────────────────────────────
*.log
logs/
# 브리핑 품질 리뷰(실 LLM) 로컬 산출물 — 커밋 금지(평가 격리)
briefing_preview_result.md

# ── 로컬 AI 작업 자료 ────────────────────────────
# Codex·Fable에 전달하는 기획서와 프롬프트는 로컬에서만 관리한다.
/CODEXPROMPT/

# ── Excel 임시파일 ───────────────────────────────
# import_mapping 작업 중 Excel이 만드는 잠금파일
~$*.xls*
.~lock.*

# ── 에디터 · OS ──────────────────────────────────
.vscode/
.idea/
.DS_Store


# 외부 데이터셋 원본·분석 산출물 — 레포에 반입하지 않는다(EdNet KT1은 CC BY-NC 4.0).
local_data/


.codex-doc-update/
ppt/
.codex_tmp/
# 🔴 백엔드 검증 시드 — 저장소에 두지 않는다 (99 #68 재조정 · 2026-08-14)
#    정본은 2026-08-13 승우님께 넘긴 전달분이다. 여기 사본을 두면 정본이 둘이 된다.
#    ⚠ students.csv 는 예외다 — 검사가 읽는 「시나리오 명세」라 추적한다.
docs/part_a/examples/seed/learning_records.csv
docs/part_a/examples/seed/detection_assignment_week_summaries.csv
docs/part_a/examples/seed/checkon_seed.sql

# 기계별 임시 조정 — 커밋하지 않는다(공통·배포 파일은 저장소가 소유한다).
docker-compose.override.yml

# 🔴 추적 밖 로컬 산출물 — 무시 규칙이 없으면 pull 이 «덮어쓴다»며 중단한다.
#   2026-08-20 실측: 로컬에만 있던 Dockerfile·.dockerignore 가 develop 신규 파일과
#   이름이 겹쳐 `git pull` 이 Aborting 했다. 규칙에 없는 로컬 파일은 그 사고를 반복한다.
# Codex 워크트리 — 도구가 만든다
.codex/
# 덮어쓰기 전 로컬 백업 보관소
.local-backup/
