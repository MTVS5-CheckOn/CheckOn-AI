# [체크온] 리포트 사양서 v0 — 월별·단건 생성 파이프라인 규격

> **지위:** member-A 초안(🟡). Open-9(전국 백분위 출처)·Open-10(15일 규칙 vs 게이트)은 백엔드 합의 대상 — A측 입장을 이 문서로 정리.
> **한 줄 원칙:** 차트 수치는 전부 코드/백엔드가 확정하고, **LLM은 해설 문장만** 쓴다.

---

## 1. 리포트 2종 — 경로·할당이 다르다

| | 월별 리포트 (`kind: report`) | 단건 리포트 (`kind: report_single`) |
| --- | --- | --- |
| 트리거 | **매월 1일 야간 배치, 전월분 일괄** — 재원생 전원 | 학부모 요청 시 강사가 수시 발행 |
| 대상 선정 | 백엔드 소유 — **15일 규칙**(15일 이후 등원생은 그 달 생략) 적용한 목록 전달 | 강사가 학생 1명 지정 |
| 할당 | **미소모**(전 유료 티어 월 1회 기본) | `report_single` 월 할당(10/30/50) + **일일 상한**(예: Standard 10/일 — 일괄 우회 방지) |
| 범위 | 전월 1개월 | 요청 시점 기준 최근 4주(기본) 또는 강사 지정 기간 |
| 승인 | 생성까지만 — 승인·전달은 강사(HITL, 발송 API 없음) | 동일 |

**Open-10 판정 흐름:** 백엔드 목록에 있어도 AI의 DataSufficiency 게이트(데이터 2주 미만)가 **우선** — 해당 학생은 `rejected_insufficient`(정상 상태, 에러 아님)로 응답하고 백엔드는 "다음 달부터 제공" 안내.

## 2. 차트 3종 사양

| # | 차트 | 데이터 소스 | 형태 | chart_ref |
| --- | --- | --- | --- | --- |
| ① | **영역별 약점 현황** | FEATURE_WEEK 집계 — 수능 6영역(`area_tag`) × 정답률, `type_tag` 보조 | 레이더(6축) + 유형 표 | `weakness_by_area` |
| ② | **보강 후 개선 추이** | 주차별 정답률 라인 + 개입(Intervention) 시점 마커 — "상담/보강 이후 오름"을 시각화 | 라인+이벤트 마커 | `improvement_trend` |
| ③ | **전국 백분위** | `benchmarks.national_percentile`(백엔드 집계 API — 출처·모수 Open-9) | 단일 지표 + 추이 | `national_percentile` |

- 차트 렌더링은 백엔드/프론트 소유 — AI는 **차트별 확정 수치 테이블**을 받아 해설만 생성.
- `item_format`별 분해(객관식 vs 서술형 정답률)는 ①의 보조 표로 — 서술형은 **강사 확정 채점만** 집계(미확정 제외).

## 3. `chart_analysis` 블록 스키마

```json
{
  "seq": 3,
  "block_type": "chart_analysis",
  "chart_ref": "improvement_trend",
  "content": "5월 셋째 주 보강 이후 문학 정답률이 58%에서 71%로 올랐습니다...",
  "evidence": [{ "source_table": "feature_week", "record_id": "fw_w21" }],
  "numbers_used": [58, 71]
}
```

- `numbers_used`: 문장 속 등장 수치 목록 — SourceGrounding이 차트 확정 수치와 **전수 대조**(하나라도 불일치 → 블록 재생성, ≤3회 후 비움).
- 블록 순서(리포트 본문 구성): 인사 → 학습 요약(fact) → 차트 해설 ①②③ → 제안 → 맺음. 각 chart_analysis는 해당 차트 바로 아래 배치.

## 4. 노출 3층 매트릭스 — 구현 기준표

| 데이터 항목 | 학생 화면 | 학부모 리포트 | 강사 화면 |
| --- | --- | --- | --- |
| 본인 정답률·추이 | ✅ | ✅ | ✅ |
| 영역별 약점 | ✅ (성장 프레임) | ✅ | ✅ |
| 전국 백분위 | ❌ **비교 일절 비노출** | ✅ (`audience: guardian`) | ✅ |
| 반 평균 | ❌ | ❌ (`audience: teacher_only`) | ✅ 내부 관리 도구 |
| 반 석차 | ❌ | ❌ | ✅ |
| 위험 신호·경보 | ❌ (낙인 방지) | ❌ (강사가 상담으로 전달) | ✅ |

**teacher_only 구조적 제외 — 구현 방식:** 컨텍스트 조립기(composition의 assembler)가 draft `kind`와 수신 대상으로 **audience 필터를 프롬프트 조립 전에 적용** — `audience: teacher_only` 항목은 LLM 입력에 아예 미포함(프롬프트에 없으면 유출 불가능). 게이트로 사후 검출하는 방어가 아니라 입력 차단이 1차. ToneSafety의 비교 표현 검출은 2차 방어.

## 5. 하위 백분위 완곡 규칙 (라벨 연동 — 매핑표 #4와 공유)

- **수치는 사실대로** — 백분위 숫자를 바꾸거나 숨기지 않는다(Evidence-first).
- **문장은 성장 추이 중심** — "하위 32%" 단독 서술 금지, 반드시 개선 추이·다음 계획과 병기.
- `sensitivity=anxious`: 완곡 +1단계(buffer_lexicon), 백분위 해설을 제안 블록 뒤로 후치.
- `comm=data`: 수치 표 중심 + 짧은 해설 / `comm=narrative`: 서사 중심 + 수치는 근거로.

## 6. 요청·응답 계약 (계약 v0.1과의 정합)

- 요청: `/v1/drafts` body에 `benchmarks`(audience 분리) 포함 — 요청JSON v0.1.1 §4 확정.
- **(7/15 확정 — BE-5)** 월별 일괄은 **배치 1회(Kafka)** — 백엔드가 대상 전체(15일 규칙 적용분)를 일괄 전달, AI는 내부에서 학생 단위 처리·부분 실패 격리 후 학생별 결과 이벤트 발행. 멱등키는 `tenant+month`(+학생별 결과 키). 스트림 스키마는 계약 v1.0 부록.
- 생성 후에도 `/refine` 핑퐁 가능(리포트도 draft) — 단 chart_analysis의 `numbers_used` 대조는 매 턴 유지.

## 7. 열린 것

- **Open-9:** 전국 백분위의 출처·모수·갱신 주기 — 콜드스타트(초기 모수 부족) 시 표시 정책(미표시 vs "참고치" 뱃지)은 백엔드·기획 결정.
- 월별 일괄의 처리 시간 상한(재원생 100명 × 블록 생성) — 야간 배치 윈도 안에 끝나는지 원가·지연 추정은 threshold 시트(#3)와 별도로 산정 필요.
