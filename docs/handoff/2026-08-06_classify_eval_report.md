# 분류 평가 실측 리포트 — 2026-08-06 (2차)

**보낸이:** 박진희(AI-A) · **러너:** `ai.evaluation.classify_eval` · **백엔드:** OpenAI(`gpt-5.4-mini`)
**관련:** 99 ⓐ·ⓔ·ⓧ · `part_a/08_evaluation_plan` §7(H-3) · 1차 `2026-08-06_llm_smoke_report.md`

## 이번 회차의 위치

1차(counsel_llm_smoke)에서 브리핑 **21/21이 400**으로 죽었고 원인이 `max_tokens` 거부였다(99 ⓤ).
회차를 나눈 덕에 **분류 88회를 태우기 전에** 잡았다. #99가 어댑터를 고쳐 develop에 들어갔으므로
이번이 **그 수정 후 첫 회차**다 — 1차 시점에 돌렸다면 전량 실패했을 88회가 **완주했다.**

⚠ 코드 수정 0줄. 이 문서는 집계만 옮긴다(원문·개별 케이스는 `local_data/`에 있고 반입 금지).

---

## 0. 착수 전 확인 4건

| # | 항목 | 결과 |
| --- | --- | --- |
| C-1 | 외부 추적 비활성 | ✅ `False ()` — 프롬프트 외부 전송 0 |
| C-2 | env 실접속(`localhost` 미포함 · `gpt-5.4-mini` · `DISABLE_THINKING=false` · 키) | ✅ 4건 전부 |
| C-3 | **1호출 프로브** — 분류 파라미터 그대로 | ✅ **200** · 439/40토큰 · 2,577ms |
| C-4 | `--date` = `2026-08-06` · 산출 경로 충돌 없음 | ✅ |

> C-3이 이번 회차의 보험이다. 1차의 교훈이 정확히 이것 — **파라미터 거부는 첫 호출에서
> 드러나는데 러너를 통째로 돌리면 88번 드러난다.**

---

## 1. H-3 합격 기준 대비 — 🔴 **미달**

| 지표 | 실측 | 기준 | 판정 |
| --- | --- | --- | --- |
| `topic` 정확도 | **93.8%** (75/80) | ≥85% | ✅ |
| `urgency` 정확도 | **87.5%** (70/80) | ≥85% | ✅ |
| **`complaint` 재현율** | **93.3%** (28/30) | **≥95%** | 🔴 **미달** |
| 인젝션 탈출 | **0/5** | 0 | ✅ |
| **종합** | | | 🔴 **미달** |

🔴 **떨어진 건 `complaint` 재현율 하나이고, 그게 하필 가장 무거운 기준이다.** §7이 이 축만
95%로 높게 잡은 이유는 **민원 놓침이 최악**이기 때문이다 — 30건 중 **2건을 놓쳤다.**

⚠ `sentiment` 축 전체 정확도는 **97.5%**(78/80)로 높다. 즉 전체는 잘 맞히는데 **complaint를
normal로 보는 방향의 오분류**가 남는다. 어느 방향으로 틀렸는지(FN인지 FP인지)는 이 러너가
집계하지 않는다 — 재현율만 낸다. **혼동행렬은 이번 회차에 없다**(99 ⓩ).

---

## 2. 🔴 confidence 구간별 실제 정확도 — ⓐ의 유일한 산출물

**축마다 따로**다(3축 독립이라 곡선도 다르다). **표본 수를 반드시 함께 본다** — 10건 미만은
근거로 쓰지 않는다.

### topic

| 구간 | 표본 | 적중 | 실제 정확도 | |
| --- | --- | --- | --- | --- |
| 0.9~1.0 | 73 | 70 | **95.9%** | |
| 0.8~0.9 | 7 | 5 | 71.4% | ⚠ 표본 부족 |

### sentiment

| 구간 | 표본 | 적중 | 실제 정확도 | |
| --- | --- | --- | --- | --- |
| 0.9~1.0 | 79 | 77 | **97.5%** | |
| 0.8~0.9 | 1 | 1 | 100.0% | ⚠ 표본 부족 |

### urgency

| 구간 | 표본 | 적중 | 실제 정확도 | |
| --- | --- | --- | --- | --- |
| 0.9~1.0 | 67 | 62 | **92.5%** | |
| 0.8~0.9 | 10 | 5 | **50.0%** | |
| 0.7~0.8 | 3 | 3 | 100.0% | ⚠ 표본 부족 |

### 이 표에서 읽히는 것 — **관측이지 결정이 아니다**

- **모델이 confidence를 높게만 쏜다.** 80건 중 **0.9 이상이 topic 73 · sentiment 79 · urgency 67**
  이다. 0.7 미만 구간은 **전 축 표본 0**이다 — 낮은 confidence 자체가 거의 안 나온다.
  ⇒ **임계값을 낮게 잡으면 아무것도 안 걸러지고, 높게 잡으면 대부분이 걸린다.** 분해능이 낮다.
- **urgency 0.8~0.9 구간이 50%**(10건 중 5)다. 표본 10건이라 경계선이지만, **높은 confidence에서
  정확도가 떨어지는 유일한 구간**이라 눈에 띈다.
- 🔴 **단조성이 없다.** urgency는 0.7~0.8(100%) > 0.8~0.9(50%) < 0.9~1.0(92.5%)로 뒤집힌다.
  캘리브레이션이 잘 된 모델이면 confidence가 오를수록 정확도도 올라야 한다. **표본이 얇아
  단정할 수 없지만, "임계값 하나로 자동 강등"이라는 설계 전제 자체를 재검토할 재료다.**

⚠ **표본 80건은 합성 코퍼스다.** 실사용자 데이터가 아니다.

---

## 3. 폴백 — ⓔ 재료

| 항목 | 값 |
| --- | --- |
| `classified=false` | **0건 / 80건 (0%)** |
| 폴백 사유 분포 | **없음** — `parse_exhausted` 0 · `tripwire_blocked` 0 |

🔴 **`tripwire_blocked` 0건이 의미 있다.** #94에서 redaction 비멱등을 봉합한 뒤 정상 문면이
차단되지 않는지가 관심사였는데, 합성 코퍼스 80건 + redaction 3건 어디서도 트립와이어가
안 걸렸다.

⚠ **폴백 0건은 "폴백률 관측이 됐다"가 아니다.** 관측할 폴백이 없었을 뿐이고, ⓔ가 요구하는
것은 **상시 집계**다(아래 판정 D).

---

## 4. 인젝션 · redaction

| 항목 | 결과 |
| --- | --- |
| 인젝션 **5건** | **탈출 0건** — 전부 enum 안으로 수렴 |
| redaction **3건** | 3건 모두 `classified=true` · 폴백 0 · confidence 0.96~0.99 |

⚠ 지시서는 인젝션 6건으로 적었으나 코퍼스 실제는 **5건**이다(`INJECTION_CASES`).

---

## 5. 축 독립성 — 관측만 (수치 기준 없음)

| 항목 | 값 |
| --- | --- |
| `topic` 오분류 | 5건 |
| 그중 **다른 축도 함께 틀린** 건 | **1건 (20%)** |

⇒ 축이 대체로 독립적으로 틀린다. 표본 5건이라 유의성은 말할 수 없다(§H-3이 "관측만"으로
정한 이유).

---

## 6. 🔴 토큰 분포 — **이번 회차에 못 쟀다**

ⓧ(상담 경로 토큰 상한)의 값을 정하려면 이 모델의 출력 토큰 분포가 필요한데 **확보하지 못했다.**

**원인:** `classify_eval`이 **실행 원장을 적재하지 않는다.** 라우터를 거치지 않고 `classify()`를
직접 부르므로 `run_store.record_run()`이 호출되지 않는다 — 게이트웨이 기본 recorder가 수집은
하지만 프로세스가 끝나며 버려진다.

```console
$ grep -n "run_store\|record_run\|default_llm_call_collector" src/ai/evaluation/classify_eval.py
→ 0건
```

**가진 것은 단일 관측 1건뿐이다**(C-3 프로브 · 추정 아님):

| 항목 | 값 |
| --- | --- |
| `prompt_tokens` | 439 |
| `completion_tokens` | **40** |
| 지연 | 2,577ms |

### reasoning 토큰 — **분리돼 있고 값은 0이었다**

벤더 usage 스키마를 1회 확인했다:

```text
completion_tokens_details: {'reasoning_tokens': 0, 'accepted_prediction_tokens': 0,
                            'audio_tokens': 0, 'rejected_prediction_tokens': 0}
```

🔴 **두 가지 사실이 나온다.**

1. **벤더는 `reasoning_tokens`를 준다.** 그런데 **어댑터가 버린다** — `TokenUsage`는
   `tokens_in`/`tokens_out`뿐이라 `completion_tokens_details`가 기록되지 않는다(99 ⓩ 신설).
2. **이 프로브에서 `reasoning_tokens`는 0이었다.** 1차 리포트와 ⓧ가 세운 가설
   ("reasoning 토큰이 예산을 삼켜 빈 응답") **의 전제가 흔들린다.**
   ⚠ 프롬프트 1건의 관측이므로 **가설을 기각하지 않는다** — 상담 프롬프트(더 길고 복잡)에서
   0이 아닐 수 있다. **가설의 참·거짓은 아직 모른다.**

⇒ **ⓧ 값을 이 리포트에서 제안하지 않는다.** 분포가 없다.

---

## 7. 실행 조건

| 항목 | 값 |
| --- | --- |
| 총 호출 | **88회**(코퍼스 80 + 인젝션 5 + redaction 3) + 사전 프로브 2회 = **90회** |
| `--interval-ms` **최종값** | **0 — 간격 없이 완주** |
| **429 발생** | **0건** |
| 원가 | 🔴 **미측정** — 대시보드가 우리 계정이 아니다. **토큰 수는 정확**하나 이번 회차 합계는 위 §6 사유로 미포착. **추정 계산은 하지 않았다**(99 ⓠ) |

🔴 **ⓡ의 우회책(`--interval-ms`)은 이번 회차에 필요하지 않았다.** 88회 순차 호출에서 429가
한 번도 안 났다 — 그 자체가 데이터다. ⚠ 다만 tier·시점 종속이라 "앞으로도 불필요"는 아니다.

---

## 8. 🔴 판정 D — 이번 회차가 닫는 것은 **0건**이다

| 항목 | 2차 결과 | 왜 못 닫나 |
| --- | --- | --- |
| **ⓐ** confidence 임계값 | ❌ **근거표만** | ⓐ가 요구하는 건 `reviewed_at NOT NULL`인 **실사용 재분류율**이다. 코퍼스 정답은 사람이 미리 만든 라벨이라 성격이 다르다. 임계값 **후보를 근거와 함께 제안**하는 것까지가 최대치이고, 이번 표는 **분해능이 낮아 후보조차 약하다**(§2) |
| **ⓔ** 폴백률 | ❌ **1회 관측치만(0건)** | ⓔ의 본체는 `runtime/metrics.py` **모듈 부재**다. 러너 수치는 일회성이고 ⓔ가 요구하는 건 **상시 집계** |
| **ⓧ** 토큰 상한 | ❌ **재료도 못 얻었다** | 분포를 재려 했으나 러너가 LLM_CALL을 적재하지 않아 **미포착**(§6). 분포를 재는 것과 값을 정하는 것은 다르고, 지금은 **재지도 못했다** |
| **㊼** seed | ❌ **범위 밖** | S5는 `counsel_llm_smoke` 소관이고 ⓧ 해소가 선행이다 |

🔴 **"해소"를 쓰지 않는다.** 근거가 생긴 것과 값이 정해진 것은 다르다 — 1차에서 ㊼를 성급히
확정했다가 되돌린 게 하루 전이다.

---

## 9. 발견한 결함 — 고치지 않고 등재만

| 기호 | 내용 |
| --- | --- |
| **ⓩ** | `classify_eval`이 LLM_CALL을 적재하지 않아 **토큰을 못 남긴다** + 어댑터가 벤더의 `completion_tokens_details`(`reasoning_tokens` 포함)를 **버린다** |
| **㉠** | 러너가 **혼동행렬을 내지 않는다** — `complaint` 재현율 미달(2건)의 방향(FN/FP)을 알 수 없다 |

---

## 10. 한계

1. **합성 코퍼스 80건**이다. 실사용자 데이터가 아니며 실사용 착수 후 재분류율로 재조정한다.
2. **1회차**다. confidence 구간의 얇은 표본(1·3·7·10건)은 회차를 더 쌓아야 한다.
3. **모델 1종**(`gpt-5.4-mini`)·**시점 1회**다.
4. **원가 미측정** — 계정 접근 불가.

---
---

# 부록 A — 혼동행렬 후속 분석 (러너 `70901ef` · 추가 호출 0회 · 8/6)

2차 본문 §1이 `complaint` 재현율 미달(93.3%)을 냈지만 **어느 방향으로 틀렸는지** 몰랐다
(99 ㉠). #102가 러너에 혼동행렬을 넣었으므로 **이미 있는 산출을 재계산**하면 답이 나온다 —
🔴 **실 LLM 호출 0회**다. 40회짜리 3차를 태우기 전에 이것부터 닫는다.

⚠ **본문 실측 수치는 한 글자도 바꾸지 않았다.** 2차 회차의 기록이고, 바뀌는 게 아니라
같은 데이터에서 파생된 것이 붙는 것이다.

## A-0. 재현 명령

```bash
uv run python - <<'PY'
import json, pathlib
from ai.evaluation.classify_eval import AXES, AXIS_LABELS, confusion_matrix, render_confusion
d = json.loads(pathlib.Path("local_data/classify_eval_result.json").read_text())
rows = [r for r in d["rows"] if r["classified"]]   # 폴백 제외(2차엔 0건)
M = {ax: confusion_matrix([(r["expected"][i], r["actual"][i]) for r in rows], AXIS_LABELS[ax])
     for i, ax in enumerate(AXES)}
for ax in AXES:
    print("\n".join(render_confusion(ax, M[ax])))
s = M["sentiment"]
print("complaint FN", s.false_negatives("complaint"), "· FP", s.false_positives("complaint"))
PY
```

🔴 **스크립트를 커밋하지 않았다.** 테스트 없는 분석 코드를 레포에 들이면 관리 대상이 하나
늘고 ㊷와 같은 자리가 생긴다 — 재현은 위 명령으로 충분하다. 집계 함수 자체는
`evaluation/classify_eval.py`에 있고 단위 테스트 17건이 붙어 있다(#102).

## A-1. 🔴 자기 검산 — 본문과 일치하는지 먼저 본다

같은 `rows`에서 나온 값이므로 **어긋나면 재계산이 틀린 것**이다. 특히 행/열을 반대로
잡으면 **FN과 FP가 정확히 뒤바뀌어** 결론이 반대가 된다.

| 검산 | 재계산 | 2차 본문 | |
| --- | --- | --- | --- |
| topic 적중 | 75/80 | 75/80 | ✅ |
| sentiment 적중 | 78/80 | 78/80 | ✅ |
| urgency 적중 | 70/80 | 70/80 | ✅ |
| complaint 재현율 | 28/30 | 28/30 | ✅ |

축 순서도 대조했다 — 산출 행의 `axes` 필드가 러너 `AXES`(`topic·sentiment·urgency`)와 같다.
폴백 제외 규약을 적용했고 2차의 `classified=false`는 **0건**이라 실제 제외분은 없다.

## A-2. 🔴 결론 — `complaint` 미달은 **전부 놓침 방향**이다

| | 건수 | 뜻 |
| --- | --- | --- |
| **FN**(complaint → normal) | **2건** | **실제 민원을 놓쳤다.** §7이 이 축만 재현율 95%로 잡은 이유가 정확히 이 방향이다 |
| **FP**(normal → complaint) | **0건** | 재현율 정의상 무관. 있었다면 강사에게 불필요한 긴장을 준다 |

```text
sentiment (행=정답 · 열=예측)
              normal  complaint
normal            50          0
complaint          2         28
```

⇒ **오검 없이 놓치기만 한다.** 모델이 민원 판정에 보수적인 쪽으로 치우쳐 있다.

## A-3. 3축 혼동행렬

```text
[topic] 총 80건 · 적중 75건
  정답 grade           → 예측 etc             : 2건
  정답 counsel_request → 예측 etc             : 1건
  정답 etc             → 예측 counsel_request : 1건
  정답 etc             → 예측 schedule        : 1건

[sentiment] 총 80건 · 적중 78건
  정답 complaint → 예측 normal : 2건

[urgency] 총 80건 · 적중 70건
  정답 immediate → 예측 normal : 10건
```

**topic** — 오분류 5건 **전부** `etc`가 관여한다(흡수처 겸 발신처). 판정 자체는 통과.

## A-4. 🔴 다수 클래스 baseline 대조 — `urgency` 기준은 **검증력이 없다**

"전부 다수 클래스로 답하는 상수 분류기"의 정확도와 실측을 나란히 둔다. 코퍼스 정답 분포는
재계산으로 실측했다(가정 아님).

| 축 | 정답 분포 | 다수 클래스 baseline | 실측 정확도 | **baseline 대비** | H-3 기준 | 기준의 baseline 대비 |
| --- | --- | --- | --- | --- | --- | --- |
| topic | 20·20·20·20 | 25.00% (균등과 동일) | 93.75% | **+68.75%p** | ≥85% | +60.00%p |
| sentiment | normal 50 · complaint 30 | 62.50% | 97.50% | **+35.00%p** | (정확도 기준 없음) | — |
| **urgency** | **immediate 15 · normal 65** | **81.25%** | 87.50% | 🔴 **+6.25%p** | ≥85% | 🔴 **+3.75%p** |

🔴 **아무것도 안 하는 분류기가 81.25%를 내는데 합격선이 85%다.** 기준이 모델을 거의 검증하지
못한다. topic·sentiment를 나란히 놓아야 **urgency만 그렇다는 것**이 보인다.

⇒ *"정확도가 통과하는 동안 긴급의 2/3를 놓쳤다"* 는 **우연이 아니라 구조적으로 예정된
결과**다. 다수 클래스(`normal` 65건)만 맞혀도 기준 근처에 닿는다.

## A-5. 🔴 `urgency` — 오분류 10건이 **전부 한 방향**이다

| | 값 |
| --- | --- |
| `immediate` 재현율 | **5/15 = 33.3%** |
| `immediate` FN(→ normal) | **10건** |
| `immediate` FP(normal →) | **0건** |

```text
urgency (행=정답 · 열=예측)
             immediate  normal
immediate            5      10
normal               0      65
```

🔴 **10건 전부 같은 방향이고 반대 방향은 0건이다 — 랜덤 노이즈가 아니라 계통 편향이다.**
노이즈라면 양방향으로 흩어진다. 프롬프트 축의 문제일 가능성이 크다.

⚠ **§7이 `sentiment`에만 재현율 기준을 둔 것이 공백이다.** 근거가 "민원 놓침이 최악"이었는데
**긴급 놓침도 같은 종류의 실패**다 — 인박스에서 긴급을 못 올린다. 99 ㉡.

## A-6. ⚠ 대응책은 여기서 정하지 않는다

- **표본이 얇다.** complaint 30건 중 2건 · immediate 15건 중 10건이다. 후자는 비율이 크지만
  절대 표본이 15건이라 기준을 세울 근거로는 부족하다.
- **프롬프트를 고치면 `prompt_version`이 올라가 2차 수치와 비교가 끊긴다.** 29/30이 나와도
  개선인지 노이즈인지 구분이 안 된다.
- ⇒ **관측만 적고 회차를 더 쌓은 뒤 판단한다.** 등재는 99 ㉡(기준 검증력)·㉢(complaint 미달)
  **두 건으로 나눴다** — 대응이 다르기 때문이다(urgency는 **기준 자체가 없고**, sentiment는
  **기준이 있고 값이 미달**이다).

## A-7. ⚠ 보고 시 표현

이번 회차를 "H-3 통과"로 쓰지 않는다. 정확한 서술은 이것이다:

> 3축 중 topic·urgency **정확도** 통과 · `complaint` **재현율 미달**(FN 2 · FP 0) ·
> **`urgency` 기준은 다수 클래스 baseline과 3.75%p 차이라 검증력 부족**(㉡)
