# [B → A] 문제생성(M2) 상태 브리핑 · 2026-08-08

> **지위:** B(염준영)가 A(박진희)에게 남기는 회전 종료 브리핑. 결정 요청과 다음 회전 계획만 담는다.
> 각 항목의 **근거는 `part_b/09`가 들고**, 여기는 가리키기만 한다 — 같은 내용을 두 곳에 두지 않는다.
>
> ⚠ **파일명 날짜는 `date`로 확인했다**(2026-08-08 토). A의 8/11 런북 오기와 같은 실수를 피하려고
> 적어 둔다 — 문서 본문의 「8/9」 표기는 A 문서의 표기를 인용한 것이고, 이 저장소 커밋일은 8/8이다.

## 1. 한 줄

**T1(문법) 수직 슬라이스는 종단으로 돌고, 나머지 4트랙과 Step3 표면은 미착수다.**
골격(게이트 3단·재시도 예산·조기 중단·원장·마스킹·상한)은 트랙과 무관하게 완성됐고,
지금 막혀 있는 것은 **자료 조달 노드 하나**와 **영속화**다.

## 2. 이번 회전에 들어간 것

| PR | 내용 | 상태 |
| --- | --- | --- |
| `#147` | 예약 태그 문 앞 400(`enqueue()` 최상단) · `weight_for()` 명시적 오류 · `generation_params` 사용 축 | 머지 |
| `#156` | 그래프 super-step 상한 유도 · ERD 대조 테이블 앵커 · 부분집합 쌍 | 머지 |
| `#157` | 09 §2-22(상한 결정 기록) · §2-23(`LexiconLookup` 소비처 0) | 머지 |
| `#160` | `POST /v1/problems` 202에 `status` 추가 · 09 §2-24 | 머지 |
| `#161` | 응답 `meta.versions` == `AI_RUN` 버전 열 (A `#20` 대응) | 열림 |

**전 구간 `2303 passed` · ruff · mypy 초록.** 각 PR의 뒤집기 결과는 본문 「확인 방법」에 있다.

## 3. A 결정·조치 대기 (우선순위 순)

| # | 항목 | 왜 B가 못 하나 | 막고 있는 것 |
| --- | --- | --- | --- |
| ① | **09 §2-20 — `PROBLEM_ITEM` 스키마 결손 12건 판정** | `db/models.py`가 **양자 승인 파일**이다 | 🔴 **영속화 전체.** 지금 인메모리라 프로세스 재시작 후 `GET` 404다. B 권고안은 `ITEM_CANDIDATE` 선례(전체 스냅숏 1컬럼 + nullable 파생 투영) |
| ② | **04 §3.11 예시 줄 개정** — 202 `data`에 `status` 추가 | 04가 A 소유 · **BE 와이어 변경** | BE가 런북 §2 규칙을 pg에 적용 |
| ③ | **런북 §2에 드레인 공백 한 행** — 「202가 `queued`면 다음 요청이 올 때까지 안 돈다」 | 런북이 A 소유 | BE가 「넣으면 곧 돈다」로 오독 |
| ④ | **추적 정본 규약** — B 제안: 99는 **색인**(미해소는 소유 무관 한 줄 필수) · `part_b/09`는 **본문** | `02_ownership.md`가 A 소유 | 「미해소 안건 전부」를 세는 방법이 안 적혀 있음 |

**답변 완료(A 대기 아님)** — B 축 셋(`target_kind`·`target_source`·`revision_kind`) 잠금 **승인** ·
`TAG_SUGGESTION.type_tag` 정본 근거는 **99 ㊣의 산출 축 판정**(pg 유추도 제안기 확인도 아님).

## 4. M2 완성도

**도는 것(T1 문법)** — 게이트 ① R-1~R-7 · ② Blind 교차 풀이 + 약점 정렬 · ③ ReleaseDecision ·
난이도 추정/밴드/재생성 1회 · 재시도 예산 3회 · `verification_unavailable` · `partial_success` ·
조기 중단 · 어문규범 대조 지식층 · 슈퍼바이저·멱등·원장·마스킹·문 앞 400·그래프 상한.
FIX-01~10 중 **8개가 코드로 서 있다**(남은 둘은 revision 표면 의존).

**안 되는 것** — T2 독서·T3 문학·T4 화법작문·T5 매체(자료 조달 노드 **0개**) ·
어휘 대조 미배선(§2-23) · Step3 표면 4종 · refine/reverify · **영속화** · 실 LLM pg 스모크(4차 이후 미실행).

> 🔴 **T1 우선은 커버리지가 아니라 기술 순서다**(`05` §1). 6월 모평 45문항 중 T1은 5문항이지만,
> 골격이 트랙과 무관해서 T2를 열 때 필요한 것은 파이프라인 재작성이 아니라 **생성 노드 하나**다.

## 5. 다음 회전 타임라인 (러프 · 2026-08-09 오후 시작)

| 시점 | 할 것 | 선행 |
| --- | --- | --- |
| **8/9 오후** | ①이 왔으면 `db/repositories/problem_store.py` 착수(조회 키·본문 없는 슬롯 표현). **안 왔으면 대기하지 않고** 대역 현실성부터 — 문면이 전부 같아 `dup_similarity_max`에 걸리는 픽스처를 고쳐 **다슬롯 성공 경로를 스위트가 처음 보게** 한다 | ① 또는 없음 |
| **8/10** | 영속화 왕복 테스트 — `save`/`get` 무손실 라운드트립 · **재시작 후 `GET` 404 해소** 확인 | ① 판정 |
| **8/11** | Step3 조회 표면 둘(`GET …/items`·`…/items/{slot_index}`) — 영속화 위에서만 의미가 있다 | 8/10 |
| **그다음** | revisions(#5) → 교체·삭제(#6). FIX-09(전체 재검증)·FIX-10(낙관적 잠금)이 여기서 처음 코드가 된다 | 8/11 |
| **범위 밖(이번 주)** | T2 생성 노드 — `05` §2.1 지문 사실성(D-04)이 선행이라 결정 전에는 시작하지 않는다 | D-04 |

⚠ **①이 늦어지면 순서가 통째로 밀린다.** 8/9 오후 기준으로 판정이 없으면 대역 현실성과
골든 대조 점검으로 반나절을 채우고, 영속화는 판정 도착 시점에 시작한다.

## 6. 저희가 아직 못 본 것 (정직하게)

- **다슬롯 성공 경로** — 대역 문면이 전부 같아 3슬롯에서 `drop_ratio_exceeded`로 멈춘다. 설계대로지만
  **`count`가 큰 정상 경로를 스위트가 한 번도 안 봤다.** A가 counsel 대역에서 겪은 것(2~25자 vs 실 300~466자)과 같은 형태다.
- **골든 대조** — `problem_eval.py`는 코퍼스의 **구조·개수·blind 불변식**을 본다. 엔진 산출 품질 대조는 아직 아니다.
- **pg 실 LLM** — 4차 이후 미실행. #147·#160·#161은 실 LLM 없이 검증되는 축이라(문 앞 400은 호출 이전, 나머지는 원장·응답) 이번 변경 검증에는 필요 없었지만, **「안 돌렸다」는 사실은 그대로다.**
