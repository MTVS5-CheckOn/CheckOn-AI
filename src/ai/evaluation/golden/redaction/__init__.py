"""redaction 골든 코퍼스 — masking_redaction.md §5 카테고리 (미탐 0 CI 게이트).

⚠ **크기를 여기 적지 않는다**(99 #02) — 정본은 `len(CORPUS)`이고 케이스가 늘 때 이 줄만
낡는다. `tests/ai/golden/test_redaction_coverage.py`가 재진술을 red로 막는다.
"""
